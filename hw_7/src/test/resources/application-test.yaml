spring:
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true

  flyway:
    url: jdbc:h2:mem:testdb
    schemas: PUBLIC
    user:
    password:
    enabled: true

  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8089