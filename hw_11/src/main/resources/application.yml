spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5433/postgres
    username: admin
    password:
#  datasource:
#    driverClassName: org.postgresql.Driver
#    url: r2dbc:postgresql://localhost:5433/postgres
#    username: admin
#    password:

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: hw_11
    generate-ddl: false
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    schemas: hw_11
    user: admin

server:
  port: 8089