spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: admin
    password: admin

  sql:
    init:
      mode: always
      data-locations: data.sql
      schema-locations: schema.sql
  h2:
    console:
      enabled: false
      path: /h2-console

server:
  port: 8089